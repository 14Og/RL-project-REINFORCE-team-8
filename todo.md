## TODO
    1. PPO model
    2. New metrics for train/test:
        * Total reward (running mean)
        * Success rate (window)
        * KL divergence of the policy
        * Policy sigma (exploration)
    3. Make gui with --no-sim plotting raw matplotlib windows (no pygame)
